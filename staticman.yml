version: 3
fields:
  - name: name
    label: Name
    type: string
    required: true
  - name: message
    label: Nachricht
    type: text
    required: true
  - name: date
    label: Datum
    type: date
    required: true
  - name: slug
    label: Slug
    type: string
    required: false # Optional, da er automatisch gesetzt wird
hooks:
  - type: add_files
    path: "_data/comments"
    files:
      - name: "{{ options.slug | downcase | replace: ' ', '-' }}.yml" # Automatisch generierter Slug aus dem Namen
        content: |
          name: "{{ fields.name }}"
          message: "{{ fields.message }}"
          date: "{{ fields.date }}"
          slug: "{{ options.slug | downcase | replace: ' ', '-' }}"
          created_at: "{{ date }}"
          updated_at: "{{ date }}"
